# ==========================================
# Perfil: integration — PostgreSQL en Docker
# Activar con: --spring.profiles.active=integration
# Requiere: docker compose up -d
# ==========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/airportdb
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=airport
spring.datasource.password=airport123

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
